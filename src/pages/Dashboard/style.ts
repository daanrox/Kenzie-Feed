import styled  from 'styled-components';


export const StyledMyPostContainer = styled.div`
    display: flex;
    flex-direction: column;
    width: 100%;
    gap: 2rem;
`
export const DashboardContainer = styled.div`
    background-color: var(--color-gray);

    padding: 3rem 0 ;

    height: 74vh;

    .container{
        height: 100%;
    }
`
